---
export interface Props {
    classList?: string;
}
const { classList } = Astro.props;
---
<div class:list={["flex items-center md:gap-9 gap-0 md:flex-nowrap flex-wrap", classList]}>
    <slot />
</div>